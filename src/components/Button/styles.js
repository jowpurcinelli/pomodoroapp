import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  text: {
    color: "#FFf",
    fontFamily: "Poppins-Regular",
    fontSize: 20,
    fontWeight: "bold",
  },
  iconContainer: {
    marginLeft: 5,
  },
});
